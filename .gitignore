node_modules/
.env
# League data never lives here — it is in ../ff-assistant-data, reached via DATA_ROOT.
# This entry is a second line of defence, not the plan.
data/
.DS_Store
reports/

# Stryker scratch; it survives a crashed run.
.stryker-tmp/
