pnpm-lock.yaml
docs/graph.md
node_modules

# Markdown is prose, hand-wrapped, and full of wide tables. Prettier pads those
# tables into aligned columns, which makes them hard to edit and every later doc
# change a noisy diff. Code is formatted; docs are written.
*.md
