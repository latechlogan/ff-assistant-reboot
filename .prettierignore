pnpm-lock.yaml
docs/graph.md
node_modules

# Markdown is prose, hand-wrapped, and full of wide tables. Prettier pads those
# tables into aligned columns, which makes them hard to edit and every later doc
# change a noisy diff. Code is formatted; docs are written.
*.md

# Generated by `pnpm fixtures` from real payloads — formatted by the generator,
# not by hand. Reformatting them would create diff noise on every regeneration.
test/fixtures/

.stryker-tmp

# Subagent worktrees: a full checkout of this repo under .claude/. Each one
# formats itself; from here they are somebody else's tree.
.claude/worktrees/
