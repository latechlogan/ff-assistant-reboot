# The only setting. Validated at startup; a missing or malformed data root fails loudly.
# Points at the private data repo: raw as-of payloads, frozen boards, bid corpus, curves.
DATA_ROOT=../ff-assistant-data
